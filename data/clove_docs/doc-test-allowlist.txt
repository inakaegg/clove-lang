# doc_examples_match_actual_results / doc_examples_oop_match_actual_results で
# 検査しないエントリ。1行1エントリ。コメント（# ...）と空行は無視する。
#
# ここへ足すときは必ず理由を書く。理由は「なぜ今この例を比較できないか」であって
# 「落ちるから」ではない。直せるものは直してから外すこと。
#
# 2026-07-26〜27 の整理で 84 → 28エントリ。経緯と分類は
# _ai/2026-07-26-doc-example-drift.md を参照。
*
#
# ---------------------------------------------------------------------------
# 1. 原理的に比較できない
# ---------------------------------------------------------------------------
#
# 実行ごとに値が変わる（channel id / agent の処理状態 / promise の解決タイミング /
# ハッシュ値）。doc例に固定値を書く形式そのものが合わない。
chan
timeout
future
promise?
promise-error
agent-send!
done?
async::scope-select
hash
#
# 実端末が要る（キー入力・raw mode）。
term::raw-mode!
term::read-key
term::with-raw-mode
#
# グローバルなテストランナーの状態に依存する（件数がスイート横断で積算される）。
test::deftest
#
# 比較する値がない。__DATA__ は複数行データ節、use は宣言。
__DATA__
use
#
# ---------------------------------------------------------------------------
# 2. 実行環境に依存する（実装が直れば外せるものを含む）
# ---------------------------------------------------------------------------
#
# 相対パスはプロセスの CWD に対して解決される。ファイル系ビルトインは
# EvalOptions::working_dir を見ずに std::fs をそのまま呼ぶため、
# cargo test（CWD = crate ディレクトリ）では data/fixtures/... を開けない。
# fixture 自体はリポジトリにあり、リポジトリルートから実行すれば例は通る。
# 直すなら「ファイル系ビルトインが working_dir に対して相対パスを解決する」。
env::parse-file
json::read-file
json::read-file-seq
yaml::read-file
toml::read-file
fs::glob
fs::glob*
#
# 外部プロセスを起動する。実行環境に echo / true があることが前提。
process::run
process::sh-line
#
# ---------------------------------------------------------------------------
# 3. 実装側の不具合・未実装（docの期待値のほうが正しい）
# ---------------------------------------------------------------------------
#
# with-redefs-fn: どんな入力でも動かない。with-redefs は特殊形式でリテラルの
# 束縛ベクタを要求するのに、std の実装は実行時の値 bindings をそのまま渡している
# （Runtime error: binding name must be symbol）。実行時に束縛を差し替える API が要る。
with-redefs-fn
#
# set::index: doc は {{:a 1} #{..}}（マップをキーにする）を期待するが、Key が
# keyword/symbol/string/number/bool しか表せないため文字列キー "{:a 1}" になる。
# Rust の Debug 表記の漏れと順序依存は修正済み（tests/composite_map_keys.rs）。
# 直すなら Key 自体が複合値を持てるようにする。
set::index
#
# infer-type: 型注釈付きリテラル ([1 2]: [Int Int]) に対して Any を返す。
# 注釈が推論へ渡っていない。
infer-type
